{{- if .Values.pilot.enabled }}
# ALB 자체의 AWS 쪽 설정 — 오늘 Ingress의 scheme/tags/load-balancer-name annotation에 해당.
# TLS/인증서는 일부러 안 넣음(파일럿은 테스트 호스트네임이라 전용 ACM 인증서가 없음, HTTP:80으로만 검증).
apiVersion: gateway.k8s.aws/v1
kind: LoadBalancerConfiguration
metadata:
  name: qket-gw-pilot-lbconfig
  namespace: {{ .Values.pilot.namespace }}
spec:
  loadBalancerName: {{ .Values.pilot.loadBalancerName }}
  scheme: internet-facing
  tags:
    {{- range $k, $v := .Values.pilot.tags }}
    {{ $k }}: {{ $v | quote }}
    {{- end }}
{{- end }}
