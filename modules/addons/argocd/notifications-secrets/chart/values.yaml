# 실제 값은 전부 modules/addons/argocd-notifications-secrets/main.tf의 helm_release가
# set 블록으로 넘겨준다 — 여기 기본값은 `helm lint`/`helm template`를 값 없이 돌려도 되게 하려는
# 자리 표시자일 뿐, 실제 배포에는 안 쓰인다.
awsRegion: ap-northeast-2
secretArn: ""
